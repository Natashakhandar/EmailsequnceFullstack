# Root .env files ignored for security
.env
.env.local
emailseq-frontend/.env

node_modules/
*.log
# dist/ - REMOVED to allow frontend build deployment to hosted server
# dist/
.DS_Store

# Local routing config
# Removed .htaccess from ignore to allow deployment
# .htaccess

node_modules
node_modules
node_modules
node_modules
