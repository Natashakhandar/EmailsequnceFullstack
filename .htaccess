<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Send ALL requests to the working Hostinger site URL
  RewriteRule ^(.*)$ https://silver-tapir-929419.hostingersite.com/$1 [P,L]

</IfModule>
